PORT=5000
MONGODB_URI=mongodb://localhost:27017/shortx
JWT_SECRET=super_secret_jwt_key_change_me_in_production
JWT_EXPIRATION=24h
CLIENT_URL=http://localhost:3000

# Redis configuration
REDIS_URI=redis://localhost:6379

# SMTP configuration for email verification / OTP / Password reset
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=placeholder_user
SMTP_PASS=placeholder_pass
SMTP_FROM=no-reply@shortx.com

# Auth Configuration
OTP_EXPIRY_SECONDS=300
RESET_TOKEN_EXPIRY_SECONDS=900

